//// using type keyword that allow multiple type for single field
// type userdata = { id: string; avatar: string; name: string };

// using interface  that allow single fixed type for single field
export interface userdata {
  id: string;
  avatar: string;
  name: string;
}
